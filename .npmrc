# This package is consumed as source (symlinked into synapse-ui), so its
# peerDependencies (@angular/*, rxjs) must not be auto-installed here — doing so
# trips an npm arborist bug (Cannot read properties of null). Skip peer resolution.
legacy-peer-deps=true
