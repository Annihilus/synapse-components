# segmented-item

## Variants

- `icon`: boolean
- `text`: boolean
- `hover`: boolean
- `disabled`: boolean
- `selected`: boolean
- `focus`: boolean

## Structure

- TEXT "text" — when `text` is `true`
- INSTANCE "icon" (component instance) — when `icon` is `true`
